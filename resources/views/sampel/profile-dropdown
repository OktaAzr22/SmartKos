<div class="relative">
    <button id="btn-profile" class="w-10 h-10 rounded-full bg-gradient-to-r from-blue-500 to-purple-500 flex items-center justify-center text-white font-bold hover:from-blue-600 hover:to-purple-600 transition-all shadow-md">
        {{ substr(auth()->user()->name ?? 'AK', 0, 2) }}
    </button>
    
    <!-- Dropdown Profil -->
    <div id="profile-dropdown" class="absolute right-0 top-12 w-80 bg-white rounded-xl shadow-lg border border-gray-200 z-40 hidden animate-dropdown">
        <div class="p-4 border-b border-gray-100">
            <div class="flex items-center">
                <div class="w-12 h-12 rounded-full bg-gradient-to-r from-blue-500 to-purple-500 flex items-center justify-center text-white text-lg font-bold mr-3 shadow-md">
                    {{ substr(auth()->user()->name ?? 'AK', 0, 2) }}
                </div>
                <div>
                    <h2 class="font-bold text-gray-800">{{ auth()->user()->name ?? 'Andi Koswara' }}</h2>
                    <p class="text-sm text-gray-600">{{ auth()->user()->email ?? 'andi.koswara@email.com' }}</p>
                </div>
            </div>
        </div>
        
        <div class="p-4 border-b border-gray-100">
            <h3 class="font-medium text-gray-700 mb-2 text-sm">Statistik Keuangan</h3>
            <div class="grid grid-cols-2 gap-3">
                <div class="text-center bg-green-50 p-2 rounded-lg border border-green-100">
                    <p class="text-xs text-gray-500">Total Saldo</p>
                    <p class="text-sm font-bold text-green-600">Rp {{ number_format($saldo ?? 0, 0, ',', '.') }}</p>
                </div>
                <div class="text-center bg-red-50 p-2 rounded-lg border border-red-100">
                    <p class="text-xs text-gray-500">Pengeluaran Bulan Ini</p>
                    <p class="text-sm font-bold text-red-600">Rp {{ number_format($pengeluaranBulanIni ?? 0, 0, ',', '.') }}</p>
                </div>
            </div>
        </div>
        
        <div class="p-4">
            <h3 class="font-medium text-gray-700 mb-2 text-sm">Riwayat Saldo</h3>
            <div class="space-y-3 max-h-40 overflow-y-auto pr-2">
                @foreach($riwayatSaldo ?? [] as $riwayat)
                <div class="flex justify-between items-center text-sm">
                    <div>
                        <p class="font-medium">{{ $riwayat['bulan'] }}</p>
                        <p class="text-xs text-gray-500">Saldo Awal: Rp {{ number_format($riwayat['saldo_awal'], 0, ',', '.') }}</p>
                    </div>
                    <p class="font-medium text-green-600">Rp {{ number_format($riwayat['saldo_akhir'], 0, ',', '.') }}</p>
                </div>
                @endforeach
            </div>
        </div>
        
        <div class="p-3 bg-gray-50 rounded-b-xl border-t border-gray-200">
            <form method="POST" action="{{ route('logout') }}">
                @csrf
                <button type="submit" class="w-full py-2 text-center text-blue-600 font-medium hover:bg-blue-50 rounded-lg transition-colors flex items-center justify-center">
                    <i class="fas fa-sign-out-alt mr-2"></i> Logout
                </button>
            </form>
        </div>
    </div>
</div>

